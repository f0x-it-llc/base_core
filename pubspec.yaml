name: base_core
description: A new Flutter project.
version: 0.5.0
homepage:

environment:
  sdk: ">=2.18.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  dartz: ^0.10.1
  rxdart: ">=0.27.1 <0.28.0"
  logger: ^2.5.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
