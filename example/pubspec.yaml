name: team_directory
description: >-
  Sample app showcasing clean architecture with clean_signals v2 and signals.
publish_to: none
version: 1.0.0

environment:
  sdk: ^3.5.0

dependencies:
  flutter:
    sdk: flutter
  clean_signals:
    path: ../
  signals: ^7.0.0
  get_it: ^9.2.1
  logger: ^2.5.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
