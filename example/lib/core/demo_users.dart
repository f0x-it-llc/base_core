/// Demo affordance: the two accounts the sample can "sign in" as.
///
/// In a real app the signed-in user id comes from your auth flow; the
/// switch-user button on the profile page exists purely to demonstrate that
/// a session change propagates to every widget reading session signals.
const demoUserIds = ['u-riley', 'u-jordan'];
